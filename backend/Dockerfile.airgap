FROM ansible-builder-backend:v5

WORKDIR /app

# Backend 코드 복사
COPY . .

# Frontend dist는 빌드 전에 backend/static으로 복사됨
# deploy.sh 스크립트에서 처리

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
